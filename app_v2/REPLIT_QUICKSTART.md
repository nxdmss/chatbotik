# 🚀 Быстрый запуск App V2 на Replit

## ✨ Автоматическая установка за 3 шага!

### Шаг 1: Добавьте модули в Replit

1. Откройте панель **Tools** (слева, иконка 🔧)
2. Нажмите **Packages**
3. Найдите и добавьте:
   - `postgresql-15`
   - `redis-7`

**ИЛИ** просто откройте файл `.replit` - модули уже добавлены!

### Шаг 2: Настройте Secrets

1. Нажмите на замочек 🔒 (слева, внизу)
2. Добавьте следующие переменные:

```
BOT_TOKEN = ваш_токен_от_@BotFather
ADMIN_IDS = ваш_telegram_id
SECRET_KEY = любая_случайная_строка_минимум_32_символа
```

**Как получить:**
- **BOT_TOKEN**: Telegram → @BotFather → `/newbot`
- **ADMIN_IDS**: Telegram → @userinfobot
- **SECRET_KEY**: Любая строка, например: `my_super_secret_key_12345678901234567890`

### Шаг 3: Создайте .env файл

В папке `app_v2` создайте файл `.env`:

```bash
# В Shell выполните:
cd app_v2
cp .env.example .env
```

Откройте `.env` и заполните:

```env
# === ОБЯЗАТЕЛЬНЫЕ ПАРАМЕТРЫ ===
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_IDS=123456789
SECRET_KEY=случайная_строка_минимум_32_символа

# === DATABASE (автоматически настроится) ===
DATABASE_URL=postgresql+asyncpg://shopbot:shopbot_pass@/.postgresql:5432/shop_db?host=/.postgresql
REDIS_URL=redis://localhost:6379/0

# === ОПЦИОНАЛЬНЫЕ ===
DEBUG=False
LOG_LEVEL=INFO
```

### Шаг 4: Запустите!

Просто нажмите зеленую кнопку **Run** ▶️

Скрипт автоматически:
- ✅ Инициализирует PostgreSQL
- ✅ Создаст базу данных `shop_db`
- ✅ Запустит Redis
- ✅ Установит все зависимости
- ✅ Создаст необходимые директории
- ✅ Запустит бота!

---

## 🔧 Ручная установка (если нужно)

### 1. Запустите скрипт установки

```bash
cd app_v2
chmod +x setup_replit.sh
./setup_replit.sh
```

### 2. Запустите бота

```bash
python main.py
```

---

## 📊 Проверка работы

### Проверить PostgreSQL:

```bash
psql -h ~/.postgresql -U shopbot -d shop_db -c "SELECT 1"
```

Должно вывести:
```
 ?column? 
----------
        1
```

### Проверить Redis:

```bash
redis-cli ping
```

Должно ответить: `PONG`

### Проверить логи:

```bash
tail -f logs/bot.log
```

---

## 🐛 Решение проблем

### Ошибка: "BOT_TOKEN не настроен"

**Решение:** Создайте файл `.env` в папке `app_v2` и заполните `BOT_TOKEN`

### Ошибка: "PostgreSQL не запускается"

**Решение:**
```bash
# Остановите старый процесс
pg_ctl -D ~/.postgresql stop

# Запустите заново
./setup_replit.sh
```

### Ошибка: "Redis connection refused"

**Решение:**
```bash
# Запустите Redis вручную
redis-server --daemonize yes

# Проверьте
redis-cli ping
```

### Ошибка: "Module not found"

**Решение:**
```bash
# Переустановите зависимости
pip install -r requirements.txt
```

---

## 💡 Полезные команды

```bash
# Посмотреть статус PostgreSQL
pg_ctl -D ~/.postgresql status

# Подключиться к БД
psql -h ~/.postgresql -U shopbot -d shop_db

# Посмотреть таблицы
psql -h ~/.postgresql -U shopbot -d shop_db -c "\dt"

# Очистить Redis
redis-cli FLUSHALL

# Посмотреть логи PostgreSQL
cat ~/.postgresql/logfile

# Перезапустить всё
./setup_replit.sh
python main.py
```

---

## 🎯 Что дальше?

1. ✅ Бот запущен
2. 📱 Найдите бота в Telegram
3. 🚀 Отправьте `/start`
4. 👨‍💼 Попробуйте админ команды: `/admin`
5. 🛍️ Добавьте товары через WebApp

---

## 📚 Дополнительно

- **Полная документация:** [app_v2/README.md](README.md)
- **Подробная установка:** [REPLIT_INSTALL.md](REPLIT_INSTALL.md)
- **Основной гайд:** [../INSTALLATION_GUIDE.md](../INSTALLATION_GUIDE.md)

---

## ✨ Готово!

**Теперь у вас работает профессиональный бот с:**
- 🐘 PostgreSQL 15
- 🚀 Redis 7
- 🏗️ Clean Architecture
- 🔒 Type Safety
- 📊 Structured Logging
- ⚡ Async/Await

**Приятной работы! 🎉**

---

*Последнее обновление: 2 ноября 2025 г.*
