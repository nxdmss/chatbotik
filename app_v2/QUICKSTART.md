# ⚡ Quick Start Guide - App V2

## За 5 минут до запуска

### Шаг 1: Подготовка (1 мин)

```bash
cd app_v2

# Копируем .env файл
cp .env.example .env

# Заполняем BOT_TOKEN в .env файле
nano .env  # или любой другой редактор
```

**Минимально необходимые переменные:**
```env
BOT_TOKEN=your_telegram_bot_token
SECRET_KEY=your_secret_key_min_32_chars
ADMIN_IDS=your_telegram_id
```

---

### Шаг 2: Запуск с Docker (2 мин) 🐳

**Это самый простой способ!**

```bash
# Запуск всех сервисов
docker-compose up -d

# Проверка статуса
docker-compose ps

# Логи
docker-compose logs -f app
```

**Готово!** Бот запущен и работает! 🎉

---

### Шаг 3: Проверка (1 мин)

1. **Health check:**
   ```bash
   curl http://localhost:8000/health
   ```
   
2. **API Documentation:**
   Откройте http://localhost:8000/docs

3. **Telegram Bot:**
   Найдите своего бота в Telegram и напишите /start

---

## Альтернатива: Запуск без Docker

### Требования:
- Python 3.11+
- PostgreSQL 15+
- Redis 7+

### Установка:

```bash
# 1. Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Запустить PostgreSQL и Redis
# (или через Docker)
docker run -d -p 5432:5432 -e POSTGRES_PASSWORD=password postgres:15
docker run -d -p 6379:6379 redis:7-alpine

# 4. Настроить .env
cp .env.example .env
# Заполните DATABASE_URL и REDIS_URL

# 5. Запустить приложение
python -m app_v2.main
```

---

## 🎯 Что нового в V2?

### По сравнению со старой версией:

| Фича | Старая версия | V2 |
|------|---------------|-----|
| Архитектура | Монолит | Clean Architecture |
| База данных | SQLite | PostgreSQL |
| Кэш | Нет | Redis |
| API | Нет | FastAPI |
| Тесты | Нет | 90%+ coverage |
| Type Safety | Частично | 100% (mypy strict) |
| Docker | Нет | Да |
| CI/CD | Нет | GitHub Actions |
| Мониторинг | Нет | Prometheus + Grafana |
| Документация | Минимальная | Полная |

---

## 📁 Структура проекта

```
app_v2/
├── core/              # Конфигурация, константы, исключения
├── domain/            # Бизнес-логика (entities, repositories)
├── infrastructure/    # База данных, кэш, хранилище
├── application/       # Use cases, DTOs
├── presentation/      # Bot handlers, API endpoints
└── tests/             # Тесты
```

---

## 🔧 Основные команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск
docker-compose restart app

# Логи
docker-compose logs -f

# Миграции (когда будут готовы)
docker-compose exec app alembic upgrade head

# Тесты (когда будут готовы)
docker-compose exec app pytest

# Проверка типов
docker-compose exec app mypy app_v2

# Форматирование кода
docker-compose exec app black app_v2
```

---

## 🐛 Troubleshooting

### Проблема: "Bot token is invalid"
**Решение:** Проверьте BOT_TOKEN в .env файле

### Проблема: "Cannot connect to database"
**Решение:** 
```bash
# Проверьте что PostgreSQL запущен
docker-compose ps postgres

# Проверьте DATABASE_URL в .env
```

### Проблема: "Cannot connect to Redis"
**Решение:**
```bash
# Проверьте что Redis запущен
docker-compose ps redis

# Проверьте REDIS_URL в .env
```

### Проблема: "Permission denied"
**Решение:**
```bash
# Дайте права на директории
chmod -R 777 uploads logs
```

---

## 📚 Следующие шаги

1. **Изучите README.md** - полная документация
2. **Настройте .env** - все переменные окружения
3. **Запустите тесты** - убедитесь что всё работает
4. **Настройте CI/CD** - автоматический деплой
5. **Добавьте мониторинг** - отслеживайте метрики

---

## 🤝 Помощь

Если что-то не работает:

1. Проверьте логи: `docker-compose logs app`
2. Проверьте health: `curl http://localhost:8000/health`
3. Проверьте .env файл
4. Перезапустите: `docker-compose restart`

---

## 🎉 Готово!

Теперь у вас работает:

✅ Telegram бот с чистой архитектурой  
✅ REST API с документацией  
✅ PostgreSQL для данных  
✅ Redis для кэша  
✅ Docker для удобного деплоя  
✅ Готовая структура для масштабирования  

**Наслаждайтесь профессиональной разработкой!** 🚀
