# 🛍️ Telegram Mini App - Интернет-магазин

[![Python 3.8+](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

Современный интернет-магазин с интеграцией Telegram Mini Apps. Полнофункциональное приложение с админ-панелью, системой заказов и поддержкой клиентов.

## ✨ Возможности

### Для клиентов
- 🛍️ Просмотр каталога товаров с фильтрацией
- 🔍 Интеллектуальный поиск с автодополнением
- 🛒 Корзина покупок с управлением количеством
- 📦 Оформление заказов через Telegram
- ⭐ Система отзывов
- 📞 Поддержка клиентов через бота
- 📱 Адаптивный дизайн для Telegram WebApp

### Для администраторов
- ➕ Добавление и редактирование товаров
- 🖼️ Загрузка и оптимизация изображений
- 📊 Статистика продаж и заказов
- 💬 Прямая связь с клиентами
- 🔔 Автоматические уведомления о заказах
- 🛡️ Защищенный доступ через переменные окружения

## 🚀 Быстрый старт

### Требования
- Python 3.8 или выше
- Telegram бот (получить через [@BotFather](https://t.me/BotFather))
- SQLite (встроен в Python)

### 1. Клонирование и установка

```bash
# Клонируйте репозиторий
git clone https://github.com/nxdmss/chatbotik.git
cd chatbotik

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка

```bash
# Скопируйте файл конфигурации
cp .env.example .env

# Отредактируйте .env и добавьте свои данные
nano .env
```

Минимальная конфигурация в `.env`:
```env
BOT_TOKEN=your_bot_token_from_botfather
ADMIN_IDS=your_telegram_id
WEBAPP_URL=http://localhost:8000
PORT=8000
```

**Как получить свой Telegram ID:**
1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш ID
3. Добавьте в `ADMIN_IDS` в файле `.env`

### 3. Инициализация базы данных

```bash
# Создаст базу данных и выполнит миграцию
python -m src.database
```

### 4. Запуск

```bash
# Запуск всего приложения
python main_new.py

# Или используйте старый main.py (будет обновлен)
python main.py
```

Приложение будет доступно по адресу: `http://localhost:8000`

## 📁 Структура проекта

```
chatbotik/
├── src/                    # Исходный код
│   ├── __init__.py
│   ├── config.py          # Конфигурация
│   ├── database.py        # Работа с БД
│   ├── models/            # Модели данных (Pydantic)
│   │   ├── product.py
│   │   ├── order.py
│   │   └── customer.py
│   └── utils/             # Утилиты
│       ├── logger.py      # Логирование
│       ├── auth.py        # Авторизация
│       └── image.py       # Работа с изображениями
├── webapp/                # Frontend
│   ├── index.html
│   ├── app.js
│   ├── styles.css
│   └── static/
│       └── uploads/       # Загруженные изображения
├── logs/                  # Логи
├── uploads/               # Загруженные файлы
├── .env                   # Конфигурация (создать из .env.example)
├── .env.example           # Пример конфигурации
├── requirements.txt       # Зависимости Python
├── main_new.py           # Главный файл (новый)
├── simple_telegram_bot.py # Веб-сервер
├── no_telegram_bot.py    # Бот поддержки
└── README.md             # Документация
```

## 🔧 Конфигурация

### Переменные окружения (.env)

| Переменная | Описание | Обязательно | По умолчанию |
|------------|----------|-------------|--------------|
| `BOT_TOKEN` | Токен Telegram бота | ✅ Да | - |
| `ADMIN_IDS` | ID администраторов (через запятую) | ✅ Да | - |
| `WEBAPP_URL` | URL веб-приложения | Нет | `http://localhost:8000` |
| `PORT` | Порт веб-сервера | Нет | `8000` |
| `DATABASE_PATH` | Путь к БД | Нет | `database.db` |
| `ADMIN_PHONE` | Телефон для оплаты СБП | Нет | `+7 (999) 123-45-67` |
| `LOG_LEVEL` | Уровень логирования | Нет | `INFO` |

### Пример .env файла

```env
# Telegram Bot
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_IDS=1593426947,987654321

# Web App
WEBAPP_URL=https://your-domain.com
PORT=8000

# Database
DATABASE_PATH=database.db

# Payments
ADMIN_PHONE=+7 (999) 123-45-67

# Logging
LOG_LEVEL=INFO
```

## 🔒 Безопасность

### ✅ Что сделано
- ✅ Админ ID вынесен в переменные окружения
- ✅ Серверная валидация всех админских операций
- ✅ Pydantic валидация входных данных
- ✅ Оптимизация и проверка изображений
- ✅ SQL-инъекции защищены через параметризованные запросы
- ✅ Проверка прав доступа на каждый API запрос

### ⚠️ Рекомендации
- Не публикуйте файл `.env` в Git (уже в `.gitignore`)
- Используйте HTTPS в продакшене
- Регулярно обновляйте зависимости
- Храните резервные копии базы данных

## 📊 База данных

### Таблицы
- `products` - Товары
- `customers` - Клиенты
- `orders` - Заказы
- `reviews` - Отзывы
- `messages` - Сообщения поддержки

### Миграция старых данных

Если у вас есть старые базы данных (`shop.db`, `customer_support.db`), они будут автоматически мигрированы:

```bash
python -m src.database
```

## 🛠️ Разработка

### Установка зависимостей для разработки

```bash
pip install -r requirements.txt
pip install pylint black pytest  # Опционально
```

### Форматирование кода

```bash
black src/
```

### Логирование

Логи сохраняются в `logs/bot.log` и выводятся в консоль:

```python
from src.utils.logger import get_logger

logger = get_logger(__name__)
logger.info("Информация")
logger.error("Ошибка")
```

### Добавление новой модели

```python
# src/models/example.py
from pydantic import BaseModel, Field

class Example(BaseModel):
    name: str = Field(..., min_length=2)
    value: int = Field(..., gt=0)
```

## 📱 Telegram WebApp

### Настройка бота

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен
3. Установите WebApp кнопку:
   ```
   /mybots -> Выберите бота -> Bot Settings -> Menu Button -> Configure menu button
   URL: https://your-domain.com
   ```

### Локальная разработка

Для локальной разработки используйте ngrok или похожий сервис:

```bash
ngrok http 8000
```

Затем обновите `WEBAPP_URL` в `.env` на ngrok URL.

## 🚢 Деплой

### Replit
1. Импортируйте репозиторий
2. Добавьте Secrets (Environment Variables)
3. Запустите `main_new.py`

### Heroku
```bash
heroku create your-app-name
heroku config:set BOT_TOKEN=your_token
heroku config:set ADMIN_IDS=your_id
git push heroku main
```

### VPS
```bash
# Клонируйте репозиторий
git clone https://github.com/nxdmss/chatbotik.git
cd chatbotik

# Установите зависимости
pip3 install -r requirements.txt

# Настройте .env
cp .env.example .env
nano .env

# Запустите через systemd или supervisor
python3 main_new.py
```

## 🤝 Поддержка

- 📧 Email: support@yourshop.ru
- 💬 Telegram: [@your_support_bot](https://t.me/your_support_bot)
- 🐛 Issues: [GitHub Issues](https://github.com/nxdmss/chatbotik/issues)

## 📄 Лицензия

MIT License - используйте свободно в коммерческих и некоммерческих проектах.

---

**Сделано с ❤️ для Telegram Mini Apps**
