#!/bin/bash
# Скрипт автоматической настройки проекта

echo "🚀 Настройка проекта LOOK & GO Shop"
echo "===================================="

# Проверка Python
if ! command -v python3 &> /dev/null; then
    echo "❌ Python 3 не найден. Установите Python 3.8 или выше"
    exit 1
fi

echo "✅ Python найден: $(python3 --version)"

# Создание виртуального окружения
if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
    echo "✅ Виртуальное окружение создано"
else
    echo "✅ Виртуальное окружение уже существует"
fi

# Активация виртуального окружения
echo "🔧 Активация виртуального окружения..."
source venv/bin/activate

# Установка зависимостей
echo "📥 Установка зависимостей..."
pip install --upgrade pip
pip install -r requirements.txt
echo "✅ Зависимости установлены"

# Создание .env если не существует
if [ ! -f ".env" ]; then
    echo "📝 Создание .env файла..."
    cp .env.example .env
    echo "⚠️  ВАЖНО: Отредактируйте .env файл и добавьте:"
    echo "   - BOT_TOKEN (получите от @BotFather)"
    echo "   - ADMIN_IDS (получите от @userinfobot)"
    echo ""
    echo "   Запустите: nano .env"
    echo ""
else
    echo "✅ .env файл уже существует"
fi

# Создание необходимых директорий
echo "📁 Создание директорий..."
mkdir -p logs
mkdir -p uploads
mkdir -p webapp/static/uploads
echo "✅ Директории созданы"

# Создание .gitkeep файлов
touch uploads/.gitkeep
touch webapp/static/uploads/.gitkeep
touch logs/.gitkeep

# Инициализация базы данных
echo "🗄️  Инициализация базы данных..."
python3 -m src.database
echo "✅ База данных инициализирована"

echo ""
echo "✅ Настройка завершена!"
echo ""
echo "📋 Следующие шаги:"
echo "   1. Отредактируйте .env файл: nano .env"
echo "   2. Добавьте BOT_TOKEN и ADMIN_IDS"
echo "   3. Запустите приложение: python3 main_new.py"
echo ""
echo "📚 Документация: README_NEW.md"
echo "🔄 Миграция: MIGRATION_GUIDE.md"
echo ""
